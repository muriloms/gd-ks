# NPM Configuration for GD-KS

# Use exact versions when saving dependencies
save-exact=true

# Set default registry
registry=https://registry.npmjs.org/

# Disable package-lock for cleaner installs
package-lock=false

# Engine strict mode
engine-strict=true
